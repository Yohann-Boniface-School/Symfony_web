<header class="grid">
    <div class="container flex">
        <a href="/">Home</a>
        <nav class="navbar flex">
            <a href="{{ path('about') }}">About</a>
            <a href="{{ path('contact') }}">Contact</a>
        </nav>
    </div>
</header>
